x <- 2
X = 4
3 -> Y

objects()
rm(x) # commande remove (supprimé)
length(Y)
mode(X)


# les lignes suivant sont importer du cours 
x <- 2 # crée l’objet x en lui donnant la valeur 2
X = 4 # X reçoit la valeur 4
b = x # b reçoit la valeur x, soit ici 2
3 -> y # y reçoit la valeur 3
