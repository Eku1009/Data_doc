#introdiction
#1-Opération et Fonction
# pour créer un vecteur "x" par exemple on utilise les attribues = ou <- 
x <- c(2,3,8,9)
x
#pour sortie des chiffres de 1 à 10 par exemple nous allons écrire:

z <- c(1:10)
z

# I.1.1. Manipulation de vecteur
#Génération des suites des nombres (fonction "seq")
a <- seq(1,9,by=2) # Allez de 1 à 10 en allant 2 à 2 
a
